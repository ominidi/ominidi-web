codecov:
  branch: master

coverage:
  precision: 2
  round: down
  range: "70...100"

  status:
    project:
      project: yes
      patch: yes
      changes: yes

comment:
  layout: "header, diff, changes, sunburst, uncovered, tree"
  require_changes: no
  behavior: default