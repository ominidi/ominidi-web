import React from 'react';
import PropTypes from 'prop-types';

const LoadMore = (props) => {
    return (
        <div className="feed__load-more"></div>
    );
};

// LoadMore.propTypes = {
//     load: PropTypes.bool
// };


export default LoadMore;